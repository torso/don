#define INSTRUCTION_H

typedef enum
{
    OP_RETURN,
    OP_BRANCH,
    OP_JUMP,
    OP_INVOKE_NATIVE,
    OP_COND_INVOKE
} ControlInstruction;

typedef enum
{
    DATAOP_NULL,
    DATAOP_TRUE,
    DATAOP_FALSE,
    DATAOP_INTEGER,
    DATAOP_STRING,
    DATAOP_PHI_VARIABLE,
    DATAOP_PARAMETER,
    DATAOP_RETURN,
    DATAOP_STACKFRAME,
    DATAOP_STACKFRAME_ABSOLUTE,
    DATAOP_EQUALS,
    DATAOP_ADD
} DataInstruction;

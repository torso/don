extern nonnull boolean CollectionIsEmpty(const RunState *state, uint object);
extern nonnull uint CollectionGetSize(const RunState *state, uint object);
extern nonnull uint CollectionGetElementValueOffset(const RunState *state,
                                                    uint object, uint index);

target default
{
    if list() == list() &&
        list(0, 100, true, false, "abc", null) == list(0, 100, true, false, "abc", null)
    {
        if list() != list(1) && list(2, 3) != list(3, 4)
        {
            echo("PASS")
        }
    }
}
